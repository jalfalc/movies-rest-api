# movies-rest-api
This is a Nodejs + Express project to do an API Rest based in a JSON file and model-view-controller pattern but without view files. It will be an Express server to de request in other frontend project.
